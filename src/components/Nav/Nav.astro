---

---

<nav><slot /></nav>

<style>
  nav {
    margin-inline: auto;
    max-inline-size: var(--page-max-inline-size);
  }

  nav {
    display: flex;
    flex-direction: row;
    justify-content: center;
    gap: 2rem;
    margin-block-end: 4rem;
  }
</style>
